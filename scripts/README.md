# Scripts

Scripts will be rebuilt after the contract tests define the supported behavior.

Phase 1 scripts must stay Colab-first and template-driven.

Current script:

- `comfy_headless_adapter.py`: validates the `adapter_request.json` generated by `ltx-runner prepare-run`, then passes `api_prompt.json` to ComfyUI's headless executor. It does not construct the LTX graph by hand.
