# Scripts

Scripts will be rebuilt after the contract tests define the supported behavior.

Phase 1 scripts must stay Colab-first and template-driven.

Current script:

- `comfy_headless_adapter.py`: validates the `adapter_request.json` generated by `ltx-runner prepare-run`. Real ComfyUI execution is enabled only after Colab-side tests are added.
